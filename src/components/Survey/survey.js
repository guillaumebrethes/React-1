//  === Import  ===;

//  === Import : local  ===;

//  === Component ===;
const Survey = () => {
  console.log('Composant Survey => ', Survey);

  return (
    <div>
      Composant Survey
    </div>
  );
};

export default Survey;
