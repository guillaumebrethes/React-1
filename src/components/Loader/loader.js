//  === Import  ===;

//  === Import : local  ===;
import './loader.scss';
//  === Component ===;
const Loader = () => {
  console.log('Composant Loader => ', Loader);

  return (
    <div className="loader" />
  );
};

export default Loader;
